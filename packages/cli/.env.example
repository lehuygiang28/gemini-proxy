# Gemini Proxy CLI Environment Configuration
# Copy this file to .env and update the values with your actual configuration

# =============================================================================
# REQUIRED: Supabase Configuration
# =============================================================================

# Your Supabase project URL (required)
# Get this from your Supabase project dashboard
# Go https://supabase.com/dashboard/ and find your project
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase service role key (required)
# Get this from: https://supabase.com/dashboard/project/your-project-id/settings/api-keys/new
# Navigate to: Project Settings > API Keys > API Keys (use new API keys, not legacy)
# Copy the service_role key (not the anon key)
# WARNING: Keep this secret and never commit it to version control
SUPABASE_SERVICE_ROLE_KEY=sb_secret_your_key

# =============================================================================
# OPTIONAL: API Keys for Sync Operations
# =============================================================================

# Gemini API Keys (optional - for sync operations)
# Supports two formats:
# 1. Comma-separated list (simple format)
# 2. JSON array with names (advanced format)

# Simple format - comma-separated API keys
# GEMINI_API_KEY=AIzaSyC...key1,AIzaSyD...key2,AIzaSyE...key3

# Advanced format - JSON array with custom names
# GEMINI_API_KEY=[{"name":"Production Key","key":"AIzaSyC..."},{"name":"Development Key","key":"AIzaSyD..."}]

# Example with actual keys (replace with your real keys):
GEMINI_API_KEY=GEMINI_API_KEY_REDACTED,GEMINI_API_KEY_REDACTED

# Proxy API Keys (optional - for sync operations)
# Supports two formats:
# 1. Comma-separated list (simple format)
# 2. JSON array with names (advanced format)

# Simple format - comma-separated proxy key IDs
# PROXY_API_KEY=gproxy_abc123def456,gproxy_ghi789jkl012

# Advanced format - JSON array with custom names
# PROXY_API_KEY=[{"name":"Client A","key":"gproxy_abc123def456"},{"name":"Client B","key":"gproxy_ghi789jkl012"}]

# Example with actual proxy key IDs (replace with your real key IDs):
PROXY_API_KEY=gproxy_abc123def4567890,gproxy_ghi789jkl0123456

# =============================================================================
# OPTIONAL: CLI Configuration
# =============================================================================

# Enable debug mode for verbose logging
# DEBUG=true

# Enable verbose logging
# VERBOSE=true

# =============================================================================
# NOTES
# =============================================================================

# 1. REQUIRED VARIABLES:
#    - SUPABASE_URL: Your Supabase project URL
#    - SUPABASE_SERVICE_ROLE_KEY: Your Supabase service role key
#
# 2. OPTIONAL VARIABLES:
#    - GEMINI_API_KEY: For syncing API keys from environment
#    - PROXY_API_KEY: For syncing proxy keys from environment
#    - DEBUG: Enable debug logging
#    - VERBOSE: Enable verbose logging
#
# 3. SECURITY:
#    - Never commit your .env file to version control
#    - Keep your service role key secret
#    - Use different keys for different environments
#
# 4. SYNC OPERATIONS:
#    - Use gproxy api-keys sync to sync from GEMINI_API_KEY
#    - Use gproxy proxy-keys sync to sync from PROXY_API_KEY
#    - Both support --dry-run to preview changes
#
# 5. FORMATS:
#    - API keys support both comma-separated and JSON array formats
#    - JSON format allows custom names for better organization
#    - Comma-separated format is simpler for basic use cases
